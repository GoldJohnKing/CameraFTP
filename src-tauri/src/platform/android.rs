use tauri::AppHandle;

/// Android foreground service stub
/// Full implementation requires additional Android-specific setup

pub fn start_foreground_service(_app: &AppHandle) {
    // Android foreground service implementation
}

pub fn stop_foreground_service(_app: &AppHandle) {
    // Android foreground service implementation
}